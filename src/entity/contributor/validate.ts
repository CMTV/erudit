import { IDataContributorInfo } from "src/entity/contributor/data";

export function validateInfo(info: IDataContributorInfo)
{
    // if (info.name)
    //     if (typeof info.name !== 'string')
    //         throw
}